Upload the ZIP contents into the existing GitHub repository root, replacing matching files, then commit. Do not upload the ZIP itself. Wait for Cloudflare Pages deployment to succeed.
Database columns are added automatically by the API; no database reset or manual schema import is required.
Features: ban/unban, session revocation, deletion with username confirmation for accounts without purchases, archive/restore with purchase history preserved. Restored accounts require approval.
Prepared and locally tested; not deployed by this package.
